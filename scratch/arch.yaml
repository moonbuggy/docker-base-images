amd64:
  TARGET_ARCH_TAG: amd64
  QEMU_ARCH:
  S6_ARCH: amd64
  QEMU_PREFIX: amd64
  PLATFORM: linux/amd64
  TAG_OVERRIDE: amd64

arm64v8:
  TARGET_ARCH_TAG: arm64v8
  EXTRA_ARCH_TAGS: arm64
  QEMU_ARCH: aarch64
  QEMU_PREFIX: arm64v8
  PLATFORM: linux/arm64/v8
  TAG_OVERRIDE: arm64v8

armv5:
  TARGET_ARCH_TAG: armv5
  EXTRA_ARCH_TAGS: arm32v5
  QEMU_ARCH: arm
  QEMU_PREFIX: arm32v5
  PLATFORM: linux/arm/v5
  BUILDER_REPO: debian
  BUILDER_TAG: 10.8-slim
  TAG_OVERRIDE: armv5

armv6:
  TARGET_ARCH_TAG: armv6
  EXTRA_ARCH_TAGS: arm32v6
  QEMU_ARCH: arm
  QEMU_PREFIX: arm32v6
  PLATFORM: linux/arm/v6
  TAG_OVERRIDE: armv6

armv7:
  TARGET_ARCH_TAG: armv7
  EXTRA_ARCH_TAGS: arm32v7
  QEMU_ARCH: arm
  QEMU_PREFIX: arm32v7
  PLATFORM: linux/arm/v7
  TAG_OVERRIDE: armv7

i386:
  TARGET_ARCH_TAG: 386
  QEMU_ARCH: i386
  QEMU_PREFIX: i386
  PLATFORM: linux/386
  TAG_OVERRIDE: 386

mips64le:
  TARGET_ARCH_TAG: mips64le
  QEMU_ARCH: mips64el
  QEMU_PREFIX: mips64le
  PLATFORM: linux/mips64le
  BUILDER_REPO: debian
  BUILDER_TAG: 10.8-slim
  TAG_OVERRIDE: mips64le

ppc64le:
  TARGET_ARCH_TAG: ppc64le
  QEMU_ARCH: ppc64le
  QEMU_PREFIX: ppc64le
  PLATFORM: linux/ppc64le
  TAG_OVERRIDE: ppc64le

riscv64:
  TARGET_ARCH_TAG: riscv64
  QEMU_ARCH: riscv64
  QEMU_PREFIX: riscv64
  PLATFORM: linux/riscv64
  BUILDER_REPO: debian
  BUILDER_TAG: sid-slim
  TAG_OVERRIDE: riscv64

s390x:
  TARGET_ARCH_TAG: s390x
  QEMU_ARCH: s390x
  QEMU_PREFIX: s390x
  PLATFORM: linux/s390x
  TAG_OVERRIDE: s390x

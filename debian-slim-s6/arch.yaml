available_keys: TARGET_ARCH_TAG QEMU_ARCH S6_ARCH QEMU_PREFIX DOCKER_FILE EXCLUDE_RELEASE

arm64v8:
  TARGET_ARCH_TAG: arm64v8
  QEMU_ARCH: aarch64
  S6_ARCH: aarch64
  QEMU_PREFIX: arm64v8
  DOCKER_FILE: Dockerfile.qemu
  EXCLUDE_RELEASE: jessie
  PLATFORM: linux/arm64/v8

amd64:
  TARGET_ARCH_TAG: amd64
  QEMU_ARCH:
  S6_ARCH: amd64
  QEMU_PREFIX: amd64
  DOCKER_FILE:
  EXCLUDE_RELEASE:
  PLATFORM: linux/amd64

armv5:
  TARGET_ARCH_TAG: armv5
  QEMU_ARCH: arm
  S6_ARCH: arm
  QEMU_PREFIX: arm32v5
  DOCKER_FILE: Dockerfile.qemu
  EXCLUDE_RELEASE:
  PLATFORM: linux/arm/v5

armv7:
  TARGET_ARCH_TAG: armv7
  QEMU_ARCH: arm
  S6_ARCH: armhf
  QEMU_PREFIX: arm32v7
  DOCKER_FILE: Dockerfile.qemu
  EXCLUDE_RELEASE:
  PLATFORM: linux/arm/v7

i386:
  TARGET_ARCH_TAG: 386
  QEMU_ARCH: i386
  S6_ARCH: x86
  QEMU_PREFIX: i386
  DOCKER_FILE: Dockerfile.qemu
  EXCLUDE_RELEASE:
  PLATFORM: linux/386

ppc64le:
  TARGET_ARCH_TAG: ppc64le
  QEMU_ARCH: ppc64le
  S6_ARCH: ppc64le
  QEMU_PREFIX: ppc64le
  DOCKER_FILE: Dockerfile.qemu
  EXCLUDE_RELEASE: jessie stretch
  PLATFORM: linux/ppc64le
